/** @type {import('next').NextConfig} */
const nextConfig = {
  // The following configurations were removed to improve code quality and performance:
  // - eslint: { ignoreDuringBuilds: true }
  // - typescript: { ignoreBuildErrors: true }
  // - images: { unoptimized: true }
  //
  // This enables ESLint and TypeScript checks during the build process and
  // turns on Next.js image optimization.
};

export default nextConfig;